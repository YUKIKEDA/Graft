namespace Graft.Core;

/// <summary>
/// Entry points for launching and connecting to instrumented applications.
/// </summary>
/// <remarks>
/// Launch / Connect behavior is added in later M2 batches. This type is a
/// public placeholder so the Core package surface exists from Batch 0.
/// </remarks>
public static class Application { }
